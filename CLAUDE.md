<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:

**Active feature (implementada, pendiente commit)**: Corrección de Layout del Menú Lateral (Sidebar Estático) y Rebranding a SYTIX — dos bugs de layout en `DashboardPage.tsx` más un rebranding textual: (1) el bloque del logo dentro del `Sider` no reaccionaba a `collapsed` (padding/tamaño fijos) y se superponía a la franja de íconos al colapsar el menú — corregido haciéndolo leer el `collapsed` ya existente (spec 038 US1) para centrar el logo y reducir su altura (30px → 16px) solo en colapsado, ya que el wordmark es el único archivo de logo (FR-008) y a 30px de alto su ancho natural (~100px) no cabía en el riel colapsado de 80px (`collapsedWidth` default de Ant Design); (2) se eliminaron los dos disparadores que forzaban la re-expansión sin clic explícito — `onMouseEnter` del `Sider` y el `setCollapsed(false)` incondicional en el `onClick` del `Menu` (ambos introducidos por spec 038) — dejando el `onCollapse` nativo (ligado al botón/flecha de alternar) como único punto de control; (3) reemplazo textual de "SyWork"/"SyWork Desk" por "SYTIX" (con eslogan "Systems | Innovation | Xcellence" en el panel de marca de Login/Reset-password, `AuthLayout.tsx`, que ya tenía una línea secundaria de copy) en los 3 puntos visibles al usuario: `frontend/index.html` (`<title>`), el header autenticado (`DashboardPage.tsx`) y `AuthLayout.tsx` — sin agregar archivos de imagen de logo nuevos, sin tocar identificadores internos no visibles (claves de `BroadcastChannel`/storage, clases CSS, `package.json`, dominio `@sywork.net` de backend, fuera de alcance por spec.md § Assumptions). Alcance restringido a componentes de layout/branding del frontend (Principio VII) — sin cambios de backend, sin dependencias nuevas, sin ejecución de suite de pruebas (instrucción explícita de esta sesión). Implementada — validada contra Docker real (reinicio de `sywork_frontend` requerido dos veces por HMR obsoleto ya conocido, ver memoria de sesión): US1 confirmado vía atributos de estilo reales del DOM tras colapsar — `Sider` en `width: 80px` (`collapsedWidth`), logo en `height: 16px` centrado, sin desbordar el riel (ancho natural del logo ~53px dentro de 80px disponibles); US2 confirmado — 20 eventos de hover/mousemove sobre el `Sider` colapsado sin ningún cambio de estado, navegación a `/my-tasks` haciendo clic en una opción del menú colapsado preservó `width: 80px` tras la navegación (antes forzaba `setCollapsed(false)`), y el control de alternar siguió expandiendo/colapsando correctamente en ambos sentidos; US3 confirmado — login muestra "SYTIX" + eslogan "Systems | Innovation | Xcellence", header autenticado y `<title>` de pestaña muestran "SYTIX", `document.body.innerText` sin ninguna coincidencia de "SyWork" en las vistas de login y autenticada, ambos `alt` de imagen del logo en "SYTIX". `tsc -b` sin errores. Commiteada (`78f4806`).
**Spec**: specs/039-sidebar-fix-sytix-rebrand/spec.md
**Plan**: specs/039-sidebar-fix-sytix-rebrand/plan.md
**Research**: specs/039-sidebar-fix-sytix-rebrand/research.md
**Data model**: specs/039-sidebar-fix-sytix-rebrand/data-model.md
**Quickstart**: specs/039-sidebar-fix-sytix-rebrand/quickstart.md
**Constitution**: .specify/memory/constitution.md

**Previous feature (implementada, pendiente commit)**: Ajustes Globales — Seguridad/Permisos, Notificaciones, Motor de SLA, Corrección de Bugs de UI y Rendimiento — paquete de 6 frentes independientes (6 User Stories P1-P3, ver spec): (US1) filtro por defecto "Asignado a mí" en Kanban/Listas/Mis Tareas para Resolutor, vista global de "Tickets" restringida a Coordinador/Admin (nuevo permiso RBAC `tickets:view_assigned`, migración `052`, sin tocar la política RLS deliberadamente permisiva de migración `012`), navegación a catálogos maestros administrativos bloqueada para Resolutor, Calendario acotado al propio recurso (`GET /resources/{id}/work-schedule` exige `resources:edit` o ser el propio recurso), menú lateral con modo colapsado; (US2) motor de SLA corregido — Contacto corre hasta "En Análisis" (antes se congelaba ya al asignar, `SLA_PHASE_FOR_STATE["contacto"]`), Resolución corre hasta "Cerrado" en vez de congelarse en "Resuelto" (reemplaza el congelamiento de specs `014`/`023`/`033`) — y entrada de auditoría inicial en el Historial de Estados al crear Ticket/Tarea (sentinel `from_status="creado"` en `ticket_status_transitions`, sin tabla nueva); (US3) diagnóstico dirigido del parpadeo del menú lateral (sin causa raíz reproducible bajo scroll/polling/timer, memoización defensiva agregada) y fix de credenciales cruzadas entre pestañas de Accesos del Cliente — nuevo `AccessCredentialForm.tsx` (una instancia de formulario por fila en vez de una compartida); (US4) cascada Cliente→Proyecto extendida a Reportes, Usuario/cliente solicitante obligatorio en Ticket/opcional en Tarea, Descripción (`note`) obligatoria en Registro de Tiempo (incluido el cronómetro del ticket); (US5) fix de "Copiar Contraseña" (TeamPage/ClientContactsPage) y correo de bienvenida HTML opcional ("Notificar al Usuario" al crear cuenta) reutilizando el mecanismo SMTP y el token/expiración de 30 min ya existente del reseteo de contraseña (spec `003`); (US6) "Ver detalle de cliente" difiere la carga de Accesos y Portafolio de software a la selección real de cada pestaña — perfilado en DevTools confirmó 4 peticiones secuenciales antes disparadas todas al abrir el modal. Sin dependencias nuevas (Principio V). Implementada — ver detalle de validación por User Story en el README (sección "Funcionalidad operativa"). `tsc -b` sin errores; extractado `CreateTicketModal.tsx` de `TicketsPage.tsx` (-345 líneas) como parte del trabajo de UI. Nota de esta sesión: el código y el README ya reflejaban esta feature como completa antes de retomar la sesión; `specs/038-ajustes-globales-seguridad-sla/tasks.md` no se marcó tarea por tarea por no tener trazabilidad exacta 1:1 desde esta sesión — no se corrió la suite de pruebas (Principio VII).
**Spec**: specs/038-ajustes-globales-seguridad-sla/spec.md
**Plan**: specs/038-ajustes-globales-seguridad-sla/plan.md
**Research**: specs/038-ajustes-globales-seguridad-sla/research.md
**Data model**: specs/038-ajustes-globales-seguridad-sla/data-model.md
**Quickstart**: specs/038-ajustes-globales-seguridad-sla/quickstart.md

**Previous feature (implementada, pendiente commit)**: Referencia a Subtareas dentro de "Clasificación" en la Tarea principal — corrige un reporte de usuario ("cuando creo una subTarea, no se muestra en la Tarea principal, que tenga subtareas relacionada en Clasificación"): la relación Tarea→Subtareas ya existía y funcionaba (tarjeta lateral "Subtareas (N)", spec 009/036) pero no se reflejaba en absoluto dentro de la Card "Clasificación" del detalle de la Tarea, dando la impresión de que la Subtarea recién creada no tenía efecto. Sin cambios de backend — `_ticket_detail` ya devuelve `subtasks` completo (spec 009); se agrega un `Descriptions.Item label="Subtareas"` en `TicketDetailPage.tsx`, justo después del ítem "Tarea Padre" (spec 036), listando cada Subtarea como hipervínculo (`Button type="link"` → `/tickets/{id}`, mismo patrón ya usado para "Registro relacionado"/"Referenciado por"/"Tarea Padre") o mostrando "Sin subtareas" cuando no hay ninguna — condicionado a `isTask && !isSubtask`, mismo guard que ya usa la tarjeta lateral. Aditivo: no reemplaza ni modifica la tarjeta lateral "Subtareas" (listado + creación). Sin dependencias nuevas, sin migración de base de datos. Implementada — validada contra Docker real (login Coordinador; Tarea TK-001290 con 2 Subtareas ya creadas en la validación de spec 036: "Clasificación" mostró el nuevo ítem "Subtareas" con ambas — TK-001291 y TK-001292 — como hipervínculos; clic en TK-001291 navegó a su detalle correctamente; Ticket normal TK-000137 confirmado sin el campo "Subtareas" en Clasificación, ya que solo aplica a Tarea sin Tarea padre propia). `tsc -b` sin errores. Requirió reiniciar `sywork_frontend` una vez antes de validar (HMR obsoleto ya conocido, ver memoria de sesión).
**Spec**: specs/037-subtareas-en-clasificacion/spec.md
**Plan**: specs/037-subtareas-en-clasificacion/plan.md
**Research**: specs/037-subtareas-en-clasificacion/research.md
**Data model**: specs/037-subtareas-en-clasificacion/data-model.md
**Quickstart**: specs/037-subtareas-en-clasificacion/quickstart.md

**Previous feature (implementada y commiteada)**: Herencia de Subtareas, Vinculación Bidireccional y Corrección de Flickering en Scroll del Ticket — (US1) al crear una Subtarea a partir de una Tarea padre, se extiende el bloque `if parent_task_id:` ya existente en `POST /api/tickets` (`backend/api/routes/tickets.py`, el mismo que hoy hereda `list_id`) para copiar también Nivel de escalamiento (`escalation_level`) y Usuario solicitante/cliente (`client_contact_id`) cuando no vienen explícitos en el payload, y para copiar el set completo de Skills requeridas de la Tarea padre invocando `TicketRepository.update_skills` justo después de crear la Subtarea (sin tabla ni columna nueva — `ticket_skills` M:N ya existente); los valores heredados quedan editables de forma independiente después (no se re-sincronizan retroactivamente). (US2) sin cambio de API: `_ticket_detail` ya devuelve `subtasks` y `SubtaskList.tsx` ya lista/navega a cada una — el "contador" pedido se deriva de `subtasks.length` en el frontend. (US3) nuevo campo aditivo `parent` (`{id, ticket_number, title}` o `null`) en `_ticket_detail`, resuelto solo cuando `ticket.parent_task_id` no es null; en `TicketDetailPage.tsx` se agrega el item "Tarea Padre" en la Card "Clasificación" como hipervínculo, reutilizando el mismo patrón ya usado ahí para "Registro relacionado"/"Referenciado por". (US4) causa raíz del parpadeo identificada: NO es CSS/`overflow-y` — es el listener de scroll de `TicketDetailPage.tsx` (líneas 88-98) que colapsa/expande el resumen de tiempo (`timeExpanded`) sin histéresis, por lo que micro-oscilaciones de scroll (rubber-band/trackpad) alternan su estado en sucesión rápida, cambiando la altura de la Card "Registros de tiempo" y empujando verticalmente "Clasificación"/"Historial de estados" debajo — se corrige agregando una zona muerta de 12px + throttle por `requestAnimationFrame` al mismo handler, sin tocar el `position: sticky` del sidebar derecho (intencional, OBS-0042/spec 030) ni la funcionalidad de colapso en sí. Sin dependencias nuevas, sin migración de base de datos. Implementada — validada contra Docker real (login Coordinador; API real: Tarea padre creada con `escalation_level="n3"`, un Usuario/cliente y 1 Skill → Subtarea creada sin esos 3 campos heredó exactamente los mismos valores, `PATCH` posterior de Skills en la Subtarea no afectó a la Tarea padre; navegador: Subtarea TK-001291 mostró "Nivel de escalamiento: N3", "Usuario/cliente solicitante: contacto_verif036" y "Skills requeridas: VERIF036" heredados sin recaptura; Card "Subtareas (2)" en la Tarea padre TK-001290 listó y navegó a ambas Subtareas; campo "Tarea Padre" visible en la Subtarea como hipervínculo — clic navegó a TK-001290 — y confirmado ausente en un Ticket sin `parent_task_id`; fix de scroll verificado con una simulación de 15 micro-oscilaciones de ±2 a ±6px vía `ResizeObserver` sobre la Card "Registros de tiempo": 0 resize events durante la secuencia, altura estable en 158px, vs. el comportamiento previo que la habría alternado en cada evento). `pytest backend/tests/api/test_tickets_subtasks.py` (2 tests nuevos de herencia, ≤2 registros de prueba c/u) en 10/10, sin correr la suite completa (Principio VII); `tsc -b` sin errores.
**Spec**: specs/036-herencia-subtareas-scroll/spec.md
**Plan**: specs/036-herencia-subtareas-scroll/plan.md
**Tasks**: specs/036-herencia-subtareas-scroll/tasks.md
**Research**: specs/036-herencia-subtareas-scroll/research.md
**Data model**: specs/036-herencia-subtareas-scroll/data-model.md
**Quickstart**: specs/036-herencia-subtareas-scroll/quickstart.md

**Previous feature (implementada, pendiente commit)**: Ajustes en Selección de Tickets en Cascada, Hipervínculos, Edición de Catálogos y Layout de Cliente — (US1) unifica el formulario de creación/edición de Ticket/Tarea (`TicketsPage.tsx`) al flujo Cliente→Proyecto→Encargado ya implementado para Tareas (reemplaza el flujo "Proyecto primero" de OBS-0045/spec 033 para Tickets, donde Cliente se auto-derivaba deshabilitado), reutilizando el filtrado por `client_id`/`project_id` ya existente; el campo "Skills requeridas" (`skill_ids`, gateado por `tickets:manage_skills`) deja de ocultarse por completo para Admin/QM. Corrección post-implementación (feedback UAT 2026-07-30): el permiso `tickets:manage_skills` en sí (antes solo Coordinador, OBS-0047/0048 spec 033) se amplía a todo rol interno — Admin, Coordinador, QM y Resolutor — vía nueva migración `051_manage_skills_all_internal_roles.py`; solo Usuario/cliente (rol externo) queda sin poder asignar/modificar Skills, tanto en la creación como en el detalle del ticket. (US2) el código de ticket/tarea pasa a ser un link clicable (patrón `<a onClick={navigate(...)}>` ya usado en `AssignmentPanelPage.tsx`) en `TicketsPage.tsx`, `MyTasksPage.tsx` y dentro de la tarjeta del tablero Kanban (`KanbanPage.tsx`, sin romper el drag-and-drop de `@hello-pangea/dnd`), preservando filtros/orden al volver atrás. (US3) `GET /api/tickets` ya soportaba `sort=urgency|created_at|-created_at|priority|-priority|status` (OBS-0028) — se agregan `-status`, `code` y `-code` a `_SORTS` (`ticket_repo.py`) y se reemplaza el indicador fijo `SortIndicator.tsx` ("por ahora fijo") por un selector real de ordenamiento en Tickets y Mis Tareas. (US4) el Modal "Detalle del cliente" de `ClientsPage.tsx` (hoy `width=820` con `Tabs` apilados) se ensancha y reorganiza en columnas (`Row`/`Col` de Ant Design, responsive nativo) sin agregar datos ni rutas nuevas. (US5) nuevo `PATCH /api/catalogs/{catalog}/{item_id}` (rename) + `CatalogRepository.rename` reutilizando el permiso ya existente `catalogs:create` (Admin/Coordinador, sin permiso ni migración nueva) — agrega botón "Editar" en `CatalogsPage.tsx` (tools/processes/resolution-types/record-types/teams/access-types); Skills (`SkillsPage.tsx`) ya tenía edición completa, queda fuera de alcance. Sin dependencias nuevas, sin migraciones de base de datos. Implementada — validada contra Docker real (login Coordinador; cascada Cliente→Proyecto→Encargado verificada end-to-end: seleccionar "Aris" dispara `GET /api/projects?client_id=<Aris>` devolviendo sus 3 proyectos, elegir "Soporte" dispara `GET /api/client-contacts?project_id=<Soporte>`, cambiar de Cliente limpia Proyecto/Encargado ya elegidos; campo Skills confirmado visible y habilitado para Coordinador; TK-000137 clicado en la lista de Tickets navegó a `/tickets/{id}` y "atrás" restauró la lista; selector de orden nuevo confirmado en Tickets y Mis Tareas reemplazando el Tag fijo de OBS-0028; Modal "Detalle del cliente" confirmado en 2 columnas a 1440px de ancho vs. 1 columna apilada antes; edición de nombre de Catálogo verificada extremo a extremo con un registro de prueba en Herramientas — `PATCH /api/catalogs/tools/{id}` 200 OK, nombre persistido tras recarga, registro desactivado al final (Catálogos no tiene borrado físico, solo activar/desactivar), sin tocar el catálogo semilla real). Bug encontrado y corregido durante la implementación (no en validación): `KanbanPage.tsx` también importaba el `SortIndicator` fijo sin wiring — al volverse un componente controlado (`value`/`onChange`) rompía la compilación; se quitó de Kanban (no estaba en alcance de esta feature extender el ordenamiento ahí, el board ya usa `sort=urgency` fijo por columna). `pytest` acotado a los archivos tocados (`test_tickets_crud.py` con 1 test nuevo de sort, `test_catalogs_rename.py` nuevo con 4 tests, `test_teams_catalog.py`, `test_record_type_catalog.py`, `test_ticket_skills.py` actualizado — Admin/Resolutor ahora pueden editar Skills, nuevo test de Usuario/cliente bloqueado) en 38/38, sin correr la suite completa (Principio VII); `tsc -b` sin errores; permiso ampliado verificado en Docker real re-logueando como Resolutor (`PATCH /api/tickets/{id}/skills` → 200 OK, antes 403). Pendiente: commit.
**Spec**: specs/035-cascada-links-catalogos-cliente/spec.md
**Plan**: specs/035-cascada-links-catalogos-cliente/plan.md
**Tasks**: specs/035-cascada-links-catalogos-cliente/tasks.md
**Research**: specs/035-cascada-links-catalogos-cliente/research.md
**Data model**: specs/035-cascada-links-catalogos-cliente/data-model.md
**Quickstart**: specs/035-cascada-links-catalogos-cliente/quickstart.md
**Constitution**: .specify/memory/constitution.md

**Previous feature (implementada, pendiente commit)**: Deshabilitación de Usuarios/Cliente y Módulo de Reportes Dinámicos (Interactive Grid) — (US1) expone y gestiona el estado Activo/Inactivo ya existente en `users.active` para cuentas de rol Usuario/cliente desde `ClientContactsPage.tsx` (columna "Estado" + filtro + acción activar/desactivar), vía nuevo endpoint acotado `PATCH /api/client-contacts/{id}/active` gateado por el permiso `client_contacts:manage` que Coordinador ya tiene — deliberadamente NO reutiliza el endpoint genérico `PATCH /api/users/{id}/deactivate` (exige `users:deactivate`, solo Admin, y aplicaría a cualquier usuario); una cuenta deshabilitada no puede iniciar sesión (ya cubierto gratis por `jwt_required_active`, que revisa `user.active` en cada request) ni ser agregada a un Proyecto nuevo (`409 contact_inactive` en `POST /api/client-contacts/{id}/projects`) ni aparecer en el selector de solicitante del ticket (`active=true` por defecto), sin alterar su historial. (US2-US6) nuevo menú "Reportes" (namespace `backend/api/routes/reports.py`, permiso nuevo `reports:view` para Admin/Coordinador/QM) con un grid interactivo (`ReportsPage.tsx`) sobre una proyección de solo lectura de Ticket+Cliente+Proyecto+Recurso+SLA+Herramienta/Proceso/Skills+tiempo registrado (`ReportRepository`, sin tabla nueva): columnas mostrables/ocultables/reordenables (`ColumnPicker.tsx`, Popover+Checkbox+`@hello-pangea/dnd` ya aprobado, sin dependencia nueva de frontend); filtros combinables por rango de fechas/Cliente/Proyecto/Encargado; agregaciones (suma/promedio/conteo) calculadas en `backend/domain/services/report_aggregation_service.py` (Capa 1, puro) sobre **todo** el conjunto filtrado (no solo la página), mostradas en una fila de totales (`Table.summary`); exportación a `.xlsx` con `openpyxl` (única dependencia nueva, backend, Principio V) respetando columnas/orden/filtros vigentes; Vistas Personalizadas guardadas por usuario (`report_saved_views`, upsert por nombre, privadas — FR-014). Bug real encontrado y corregido durante la validación en navegador: axios serializaba los parámetros repetibles (`aggregate`, `columns`) como `aggregate[]`/`columns[]`, que Flask no reconoce (`request.args.getlist` esperaba la clave sin corchetes) — se corrigió `reportService.ts` para construir `URLSearchParams` manualmente (mismo patrón ya usado en `ticketService.panel()`), en vez de pasar arrays al `params` de axios. Otro bug encontrado por un script de verificación E2E (no un test automatizado): faltaba `from flask import request` en el nuevo endpoint `PATCH /active`. Implementada — validada contra Docker real (login como Admin; filtro por Cliente "Aris" verificado end-to-end con `client_id` correcto en la request; agregación "Suma" sobre Tiempo registrado verificada mostrando `{"total_logged_minutes": {"sum": 0}}` y reflejada en la fila de totales; exportación a Excel verificada con `columns=` repetidos y 200 OK; Vista Personalizada guardada y recuperada con columnas/filtros/agregaciones intactos; US1 verificado end-to-end con un script desechable: crear→desactivar→409 al repetir→bloqueo de nuevo proyecto→filtro `active=false/true`→reactivar→alta en proyecto nuevo, todo OK — sin tocar las cuentas semilla reales `Eliseon@aris.ming.com` et al., solo se abrió y canceló su modal de confirmación); `pytest` acotado a los archivos tocados (`test_report_aggregation_service.py`, `test_reports_aggregate.py`, `test_reports_export.py`, `test_client_contacts_api.py` con 3 tests nuevos de US1, `test_client_contacts_projects.py`) en 35/35, sin correr la suite completa (Principio VII); `tsc -b` sin errores. Pendiente: commit.
**Spec**: specs/034-usuarios-inactivos-reportes-grid/spec.md
**Plan**: specs/034-usuarios-inactivos-reportes-grid/plan.md
**Tasks**: specs/034-usuarios-inactivos-reportes-grid/tasks.md
**Research**: specs/034-usuarios-inactivos-reportes-grid/research.md
**Data model**: specs/034-usuarios-inactivos-reportes-grid/data-model.md
**Quickstart**: specs/034-usuarios-inactivos-reportes-grid/quickstart.md

**Previous feature (completada, pendiente commit y retest formal del consultor UAT)**: Cierre de OBS-0044–OBS-0059 (Backlog UAT ITER-008, barrido E2E de Emilio Vargas) — corrige las 16 observaciones en estado `Abierta` de `UAT/02_Backlog/BACKLOG.md`, agrupadas en 9 historias: (US1, OBS-0044/0056/0057) los toasts de antd v5 no se veían porque faltaba envolver la app en `<App>` — se agregó y `LoginPage`/`CommentComposer` pasan a `App.useApp()`; la causa real de que el login "no mostrara nada" era además un segundo bug en el interceptor de `apiClient.ts` que redirigía a `/login` ante CUALQUIER 401 (incluida la propia respuesta de credenciales inválidas), gateado ahora con `hadAuthHeader`; el comentario que dispara una transición ahora dice `Ticket movido a "<Estado>"` en vez de "Comentario registrado"; (US2, OBS-0055) `validate_not_future` comparaba `work_date` (calculado en hora local del recurso) contra `date.today()` del servidor — unificado a la misma base horaria; (US3, OBS-0050/0054) `TicketTimerWidget`/`TicketWorkSessions` ocultan el cronómetro para usuarios sin recurso (`no_resource_profile`) en vez de mostrar un toast confuso; (US4, OBS-0052/0053) nuevo endpoint `GET /api/tickets/qm-candidates` lista QM por rol (sin requerir perfil de recurso) para el modo "Pre-Análisis" del panel de asignación, con validación backend de `assignee_role_mismatch` si el rol no coincide con el modo (`resolver`→Resolutor, `pre_analysis`→QM), usando `ResourceRepository.get_or_create_for_user` para aprovisionar el recurso del QM de forma perezosa; (US5, OBS-0045/0046) Proyecto y Usuario/cliente pasan a obligatorios al crear un Ticket (no Tarea) desde perfil interno, con Cliente auto-derivado (Select deshabilitado) y bloque de SLA aplicable mostrados al elegir Proyecto — autoservicio y Tareas conservan el comportamiento previo; (US6, OBS-0047/0048/0049) nueva tabla puente `tool_processes` (M:N, administrable desde Catálogos) sugiere — sin restringir — el Proceso una vez elegida la Herramienta en el formulario de ticket (grupos "Sugeridos"/"Otros"); las Skills requeridas se sugieren igual reutilizando los `tool_id`/`process_id` ya existentes en el catálogo de Skills (sin tabla nueva); nuevo permiso `tickets:manage_skills` (solo Coordinador) reemplaza a `tickets:edit` como gate de edición de Skills requeridas, tanto en el detalle del ticket como en el propio formulario de creación (Admin/QM quedan en solo lectura); (US7, ya cubierto por US1) confirmación visible del nuevo estado; (US8, OBS-0059) nuevos campos `sla_execution_result`/`sla_execution_consumed_seconds` (análogos a los de Contacto) se congelan al transicionar a resuelto/cerrado/cancelado si la fase Ejecución llegó a correr, expuestos en `TicketSlaState` y mostrados en `SlaCounter` junto a Contacto; (US9, OBS-0051/0058) el contador "En progreso" se renombra a "Activos" con subtítulo aclaratorio, y el badge de prioridad muestra el nombre legible (`PRIORITY_LABELS`) en vez de "P1..P4". Sin dependencias nuevas. Implementada — validada contra Docker real (creación de ticket con Proyecto→Cliente/SLA auto-derivados verificada end-to-end vía TK-000506; vínculo Herramienta↔Proceso creado y verificado filtrando el selector de Proceso en grupos Sugeridos/Otros; permiso `tickets:manage_skills` verificado ocultando/mostrando el campo de Skills según rol; ciclo completo nuevo→contacto→en_analisis→en_ejecucion→resuelto verificado mostrando `Fase: Cerrado · Contacto: Cumplido · Ejecución: Cumplido` en el detalle del ticket); `pytest` 574/576 (2 fallos preexistentes no relacionados, confirmados por diff limpio); `tsc -b` sin errores; `OBS-0044`–`OBS-0059` actualizadas a `Lista para Validar` en el backlog. Pendiente: commit y retest formal por el consultor UAT (autor de las observaciones: Emilio Vargas).
**Spec**: specs/033-cierre-obs-0044-0059/spec.md

**Previous feature (completada, pendiente commit y retest formal del consultor UAT)**: Cierre de OBS-0060–OBS-0063 (Backlog UAT ITER-009, reportadas por Arely Pazmiño) — corrige 4 observaciones de `UAT/02_Backlog/BACKLOG.md`: (1) OBS-0060, hora del historial de registros de tiempo desfasada ~5h respecto a la ingresada — causa raíz confirmada: `TimeLogModal.tsx`/`WorkSessionForm.tsx` hacían `iso.slice(11,16)` sobre el ISO-8601 en UTC devuelto por la API en vez de formatear a hora local (el dato en `work_sessions.started_at/ended_at` ya se guardaba correcto); ahora usan `date-fns` (`format(parseISO(...), 'HH:mm')`); (2) OBS-0061, la etiqueta "Fuera de jornada" se superponía al horario en la columna "Fecha" del historial — reubicada a la columna "Horario", debajo del texto del horario; (3) OBS-0062, no se notificaba al reasignar un ticket (`POST /api/tickets/{id}/reassign` nunca invocaba `NotificationService`, a diferencia de `/assign` que sí lo hace) — se agregó `event_type` `"reassigned"` y mensaje enriquecido (cliente, prioridad, estado, quién asignó) en ambos eventos; (4) OBS-0063, se podía asignar/reasignar un ticket a un resolutor con la cuenta de usuario desactivada — `AssignmentService`/`ReassignmentService` ya validaban `Resource.active` pero no el `User.active` vinculado (`Resource.user_id`), dos toggles independientes en `TeamPage.tsx` ("Desactivar recurso" vs. "Desactivar cuenta"); se agregó `Resource.user_active` (poblado vía `relationship` `viewonly` a `UserModel`) y el chequeo en ambos servicios + filtro en `useResourceCandidates.ts` + campo nuevo en `GET /api/resources`. Sin dependencias nuevas, sin migración de base de datos. Implementada — tasks 22/22, validada contra Docker real (OBS-0060/0061 verificadas registrando tiempo real en TK-000001 vía navegador logueado: historial y formulario de edición muestran la hora exacta ingresada sin desfase, etiqueta "Fuera de jornada" ya no se superpone; OBS-0063 verificada end-to-end vía `GET /api/resources` desde el navegador, confirmando `user_active: false` para un recurso con cuenta desactivada); `pytest tests/domain/test_notification_service.py tests/domain/test_assignment_service.py tests/domain/test_reassignment_service.py tests/api/test_reassign.py` en 20/20; `tsc -b` sin errores; `OBS-0060`–`OBS-0063` actualizadas a `Lista para Validar` en el backlog. **Nota de renumeración**: se preparó originalmente como `ITER-008`/`OBS-0044`–`OBS-0047`; al integrar con `main` (`git pull`) colisionó con un `ITER-008`/`OBS-0044`–`OBS-0059` ya mergeado en paralelo (barrido E2E de Emilio Vargas, PR #38) — se renumeró a `ITER-009`/`OBS-0060`–`OBS-0063` (contenido idéntico, solo cambian los IDs) y se renombró el directorio de spec (`032-cierre-obs-0044-0047` → `032-cierre-obs-0060-0063`); ver nota completa en `UAT/01_Iterations/ITER-009/ITER-009.md`. Pendiente: commit y retest formal por el consultor UAT (dueña de las observaciones: Arely Pazmiño).
**Spec**: specs/032-cierre-obs-0060-0063/spec.md
**Plan**: specs/032-cierre-obs-0060-0063/plan.md
**Tasks**: specs/032-cierre-obs-0060-0063/tasks.md
**Research**: specs/032-cierre-obs-0060-0063/research.md
**Data model**: specs/032-cierre-obs-0060-0063/data-model.md
**Quickstart**: specs/032-cierre-obs-0060-0063/quickstart.md
**Constitution**: .specify/memory/constitution.md

**Previous feature (implementada, pendiente commit y retest formal del consultor UAT)**: Cierre de OBS-0041 (Backlog UAT ITER-006, propuesta de Camilo Reyes) — amplía el modelo de "Accesos y conexiones" del Cliente (spec `018-cliente-accesos-conexiones`) con 4 piezas que comparten una sola migración: (1) `access_type` deja de ser un enum fijo de código y pasa a ser una FK a un nuevo catálogo administrable `catalog_access_types` (mismo patrón `_CatalogMixin` que `catalog_teams` de OBS-0024, más un `color_index` auto-asignado y estable; semilla VPN/Base de datos/Servidor-instancia/Escritorio remoto/Sistema-Integración); (2) credenciales múltiples por acceso vía nueva tabla hija `client_access_credentials` (1 acceso ──< N credenciales), migrando automáticamente `username`/`password` ya cargados como primera credencial (`label='Principal'`); (3) `client_access` gana `port` propio (separado del host) y `environment` deja de restringirse a un solo tipo; (4) `client_access_attachments` gana `client_access_id` (FK opcional) para anclar un adjunto a un acceso puntual, sin perder los adjuntos generales de cliente ya existentes. Sin dependencias nuevas, sin cambio de mecanismo de cifrado ni de RLS (se replica el patrón app-level ya vigente en `client_access`). Implementada — tasks 29/29, migración `047_client_access_catalog_credentials` aplicada y validada contra Docker real (accesos migrados conservan tipo VPN y credencial `Principal`; tipos nuevos APEX/NetSuite con color estable; credenciales múltiples independientes vía UI expandible; puerto/ambiente universal; adjunto anclado a un acceso puntual, sin mezclarse con los generales); `pytest tests/api/test_client_access_api.py tests/api/test_client_access_credentials_api.py` en 9/9; `tsc -b` sin errores; `OBS-0041` actualizada a `Lista para Validar` en el backlog. Pendiente: commit y retest formal por el consultor UAT (dueño de la observación: Camilo Reyes).
**Spec**: specs/031-cliente-accesos-ampliado/spec.md
**Plan**: specs/031-cliente-accesos-ampliado/plan.md
**Tasks**: specs/031-cliente-accesos-ampliado/tasks.md
**Research**: specs/031-cliente-accesos-ampliado/research.md
**Data model**: specs/031-cliente-accesos-ampliado/data-model.md
**Quickstart**: specs/031-cliente-accesos-ampliado/quickstart.md

**Previous feature (implementada, pendiente commit y retest formal del consultor UAT)**: Cierre de OBS-0042/OBS-0043 (Backlog UAT ITER-007) — corrige las 2 observaciones en estado `Abierta` de `UAT/02_Backlog/BACKLOG.md` (`OBS-0042`, `OBS-0043`, reportadas por Juan Murcia en `ITER-007`): (1) reorganiza el layout del Detalle del Ticket (`TicketDetailPage.tsx`) intercambiando las Cards "Clasificación" y "Comentarios y acciones" entre columnas, y da al historial de comentarios (`CommentThread`) su propio contenedor con scroll interno acotado, mientras la caja de nuevo comentario y las acciones de estado permanecen visibles; (2) expone el cliente del proyecto en SLA Configurable para distinguir proyectos homónimos (ej. "Soporte" de Aris vs. Vaxthera) — reutiliza `client_id`/`client_name` ya existente en `ProjectListItem` para el selector "Filtrar por proyecto" y el formulario, y amplía de forma aditiva el serializador de `SlaRule` (`backend/api/routes/sla_rules.py`) agregando `client_id`/`client_name` para mostrar una columna "Cliente" en la tabla. Sin nuevas dependencias ni migración de base de datos. Implementada — tasks 14/14, validada contra Docker real (layout con sticky/scroll interno confirmado por métricas de DOM; Cliente visible en filtro/formulario/tabla de SLA con Aris/Vaxthera); pytest acotado a `test_sla_rules.py` en 13/13; `OBS-0042`/`OBS-0043` actualizadas a `Lista para Validar` en el backlog. Pendiente: commit y retest formal por el consultor UAT.
**Spec**: specs/030-cierre-obs-0042-0043/spec.md
**Plan**: specs/030-cierre-obs-0042-0043/plan.md
**Tasks**: specs/030-cierre-obs-0042-0043/tasks.md
**Research**: specs/030-cierre-obs-0042-0043/research.md
**Data model**: specs/030-cierre-obs-0042-0043/data-model.md
**Quickstart**: specs/030-cierre-obs-0042-0043/quickstart.md

**Previous feature (en Draft, pendiente `/speckit-tasks` y ejecución)**: Ampliación de Datos Semilla (Usuarios por Rol, Resolutores con Skills y Verificación de Calendarios) — nuevo script idempotente `backend/scripts/seed_dev_users.py` (patrón de `seed_clients_aris_vaxthera.py`, sin migración de Alembic) que agrega 2 usuarios adicionales por cada uno de los 4 roles internos (Admin, Coordinador, QM, Resolutor), garantiza un mínimo de 4 usuarios con rol Resolutor (el ya existente + 3 nuevos), cada uno con perfil de Recurso y 3 skills asignadas del catálogo existente (8 skills), todos con la contraseña estándar ya usada por el seed base (`SyWork_Dev2026!`); reescribe `docs/credenciales_dev.txt` como documento único y autoritativo de todas las credenciales sembradas (roles internos + Usuario/cliente de Aris/Vaxthera), eliminando la fila de un fixture de pruebas automatizadas (`contacto.demo@clienteexterno.com`, exclusivo de `backend/tests/conftest.py`) que no corresponde a ningún seeder real; y verifica/corrige la zona horaria de los clientes Aris (`America/Bogota`) y Vaxthera (`America/Guayaquil`). En Draft — spec y plan completos, pendiente `/speckit-tasks` y ejecución.
**Spec**: specs/029-seed-usuarios-roles-skills/spec.md
**Plan**: specs/029-seed-usuarios-roles-skills/plan.md
**Research**: specs/029-seed-usuarios-roles-skills/research.md
**Data model**: specs/029-seed-usuarios-roles-skills/data-model.md
**Quickstart**: specs/029-seed-usuarios-roles-skills/quickstart.md

**Previous feature (en Draft, pendiente ejecución)**: Cierre de observaciones "Abierta" del Backlog UAT (SLA, Tickets, Calendario) — corrige las 12 observaciones en estado `Abierta` de `UAT/02_Backlog/BACKLOG.md` (`OBS-0029`–`OBS-0040`, iteraciones `ITER-004`/`ITER-005`): motor de SLA no siempre calendario-consciente al recalcular por cambio de estado o creación fuera de horario laboral (causa raíz: `resource`/calendario es parámetro opcional en `sla_service`, algún call-site no lo pasa); cronómetro no se detiene ni bloquea al cerrar un ticket (el chequeo solo existía en `finish()`, no en `start()`); título de ticket sin validar espacios en blanco ni emojis; formulario de SLA Configurable sin validación explícita de mínimo/máximo (dependía del clamp silencioso de `InputNumber`) ni máximo definido (ahora 21600 min / 15 días); calendario de un recurso interno sin mostrar su jornada laboral/ausencias (datos ya expuestos por `calendarService`, falta wiring); registro de tiempo fuera de horario ahora se permite y se clasifica como "fuera de jornada" (nuevo campo `off_hours` en `WorkSession`), y se corrige el bug de fecha (`work_date` tomaba `date.today()` del servidor en UTC en vez de la fecha local del recurso). En Draft — spec, plan y tasks completos (33 tareas / 6 User Stories), pendiente ejecución (`/speckit-implement` o manual).
**Spec**: specs/028-cierre-backlog-uat-abierto/spec.md
**Plan**: specs/028-cierre-backlog-uat-abierto/plan.md
**Tasks**: specs/028-cierre-backlog-uat-abierto/tasks.md
**Research**: specs/028-cierre-backlog-uat-abierto/research.md
**Data model**: specs/028-cierre-backlog-uat-abierto/data-model.md
**Quickstart**: specs/028-cierre-backlog-uat-abierto/quickstart.md

**Previous feature (en curso)**: Configuración de Entornos Aislados (Test y Producción) en Docker Compose — arquitectura de despliegue en el servidor Ubuntu para correr Test y Producción de forma simultánea e independiente sobre la misma máquina: un único `docker-compose.yml` parametrizado (puertos, `container_name` y `VITE_API_URL` por variable, no dos archivos duplicados; secretos sin default como `JWT_SECRET`/`POSTGRES_PASSWORD` con `${VAR:?...}` para fallar explícito si faltan) + namespacing nativo de Compose por nombre de proyecto (`-p sywork_test`/`-p sywork_prod`) para aislar red/volumen; puertos diferenciados (Test `8080` app / `3001` API / `5433` BD / `6380` Redis; Producción `80` app / `3000` API / `5432` BD / `6379` Redis, todos con default = valores actuales de desarrollo); archivos `.env.test`/`.env.prod` (+ plantillas `.env.test.example`/`.env.prod.example` trackeadas) agregados a `.gitignore`; guía de arranque/parada/logs por ambiente en el README. La terminación TLS/HTTPS de Producción y los Dockerfiles de grado producción (gunicorn/build estático) quedan fuera de alcance (pendientes, ver `docs/GUIA_DESPLIEGUE_SYWORK_TICKETS.txt` y el `TODO(HOSTING)` de la Constitución). Implementada — tasks 17/17 (T001-T017); validado en vivo contra Docker real: Test (`sywork_*_test`) levantado, `VITE_API_URL`/`TZ` conectados correctamente, aislamiento de nombres/red/volumen confirmado frente al stack de dev sin tocarlo, fallo explícito verificado para archivo `.env.*` faltante y para variable obligatoria faltante (`JWT_SECRET`). Producción solo se validó por `docker compose config` (sin `up` real, para no chocar con el stack de dev de esta máquina que reutiliza los mismos puertos `5432`/`6379`) — **pendiente**: repetir el `up` real de `sywork_prod` en el servidor Ubuntu de destino antes de aceptar como Delivery.
**Spec**: specs/027-docker-entornos-aislados/spec.md
**Plan**: specs/027-docker-entornos-aislados/plan.md
**Tasks**: specs/027-docker-entornos-aislados/tasks.md
**Research**: specs/027-docker-entornos-aislados/research.md
**Data model**: specs/027-docker-entornos-aislados/data-model.md
**Quickstart**: specs/027-docker-entornos-aislados/quickstart.md

**Previous feature (completada)**: Script de Datos Semilla (Seeders/Fixtures) — Clientes Aris y Vaxthera — un único script `backend/scripts/seed_clients_aris_vaxthera.py` (patrón de `seed_tickets.py`, sin migración de Alembic) que crea de forma idempotente los clientes Aris (Colombia, `America/Bogota`) y Vaxthera (Ecuador, `America/Guayaquil`), sus proyectos (Aris: Evolutivo/Preventa sin SLA y Soporte con matriz de SLA de 4 niveles; Vaxthera: Soporte explícitamente sin SLA), los 3 usuarios "Usuario/cliente" (`Eliseon@aris.ming.com`, `paulaBlanco@aris.ming.com`, `pablo@vaxthera.com`) y las Listas de Tareas de cada proyecto Soporte (8 para Aris, 5 para Vaxthera).
**Spec**: specs/026-seed-clientes-proyectos/spec.md
**Plan**: specs/026-seed-clientes-proyectos/plan.md
**Research**: specs/026-seed-clientes-proyectos/research.md
**Data model**: specs/026-seed-clientes-proyectos/data-model.md
**Quickstart**: specs/026-seed-clientes-proyectos/quickstart.md

**Previous feature (completada)**: Actualización Integral del Manual de Usuario (v3.2, alcance ampliado a toda la app) — `docs/Manual_de_Usuario.md` y `docs/Manual_de_Usuario.docx` con resumen arquitectónico, 3 diagramas Mermaid (ciclo de vida del Ticket, aprobación de vacaciones/permisos, pausa/reanudación de SLA) y guía paso a paso de las 12 áreas de pantallas (Tickets, Kanban, Mis Tareas, Panel de Asignación, Detalle de Ticket, Vista Usuario/cliente, RRHH, Registro/Reporte de Tiempos, los 8 Maestros, Mi Perfil, Login/Reset), verificada navegando la app real en Docker
**Spec**: specs/025-manual-usuario-integral/spec.md
**Plan**: specs/025-manual-usuario-integral/plan.md
**Research**: specs/025-manual-usuario-integral/research.md
**Data model**: specs/025-manual-usuario-integral/data-model.md
**Quickstart**: specs/025-manual-usuario-integral/quickstart.md

**Previous feature (completada)**: Sugerencias de Carga y Disponibilidad en la Reasignación — el selector de nuevo resolutor en "Reasignar" (spec 023) muestra la misma carga de trabajo, orden por menor carga y etiqueta de no disponibilidad (fuera de horario/festivo/ausencia) que ya tiene la asignación inicial (Triage Push)
**Spec**: specs/024-reasignacion-sugerencias-carga/spec.md
**Plan**: specs/024-reasignacion-sugerencias-carga/plan.md
**Research**: specs/024-reasignacion-sugerencias-carga/research.md
**Data model**: specs/024-reasignacion-sugerencias-carga/data-model.md
**Quickstart**: specs/024-reasignacion-sugerencias-carga/quickstart.md

**Previous feature (completada)**: Ajuste Visual en Historial de Estados (SLA) y Reasignación de Resolutores — tiempo transcurrido e indicador de cumplimiento SLA (✅/⚠️/❌) por cada cambio de estado, y reasignación de Ticket/Tarea a otro resolutor con registro visible "resolutor anterior ➡️ nuevo resolutor"
**Spec**: specs/023-historial-sla-reasignacion/spec.md
**Plan**: specs/023-historial-sla-reasignacion/plan.md
**Research**: specs/023-historial-sla-reasignacion/research.md
**Data model**: specs/023-historial-sla-reasignacion/data-model.md
**Quickstart**: specs/023-historial-sla-reasignacion/quickstart.md

**Previous feature (completada)**: RRHH — Franjas Horarias globales por país (herencia + modo Personalizado), Calendario de Equipo Superpuesto (Mes/Semana/Día, ausencias parciales por horas) y Motor de SLA Dinámico basado en disponibilidad real
**Spec**: specs/022-rrhh-calendario-sla-dinamico/spec.md
**Plan**: specs/022-rrhh-calendario-sla-dinamico/plan.md
**Research**: specs/022-rrhh-calendario-sla-dinamico/research.md
**Data model**: specs/022-rrhh-calendario-sla-dinamico/data-model.md
**Quickstart**: specs/022-rrhh-calendario-sla-dinamico/quickstart.md

**Previous feature (completada)**: Festivos sincronizados por API pública, categorización visual (Oficial vs. Regional/Religioso) y cumpleaños de Recursos en el Calendario
**Spec**: specs/021-festivos-api-cumpleanos/spec.md
**Plan**: specs/021-festivos-api-cumpleanos/plan.md
**Research**: specs/021-festivos-api-cumpleanos/research.md
**Data model**: specs/021-festivos-api-cumpleanos/data-model.md
**Quickstart**: specs/021-festivos-api-cumpleanos/quickstart.md

**Previous feature (completada)**: Fase 5 SDD V3 — Calendarios multi-zona horaria con festivos por país, horario laboral semanal, gestión de vacaciones/permisos con doble aprobación (Jefe directo + rol RRHH) y alerta de disponibilidad (sin bloquear) al asignar tickets
**Spec**: specs/020-calendarios-vacaciones-disponibilidad/spec.md
**Plan**: specs/020-calendarios-vacaciones-disponibilidad/plan.md
**Research**: specs/020-calendarios-vacaciones-disponibilidad/research.md
**Data model**: specs/020-calendarios-vacaciones-disponibilidad/data-model.md
**Quickstart**: specs/020-calendarios-vacaciones-disponibilidad/quickstart.md

**Previous feature (completada)**: Unidades de tiempo (minutos/horas/días) al configurar SLA — el campo "Tiempo límite de diagnóstico, análisis y ejecución" acepta horas/días y convierte a minutos
**Spec**: specs/019-sla-unidades-tiempo/spec.md
**Plan**: specs/019-sla-unidades-tiempo/plan.md

**Previous feature (en curso)**: Accesos y conexiones múltiples del Cliente (VPN/URL por ambiente/Escritorio remoto) en Maestros > Clientes — resuelve OBS-0001/OBS-0008/OBS-0017 del framework UAT
**Spec**: specs/018-cliente-accesos-conexiones/spec.md
**Plan**: specs/018-cliente-accesos-conexiones/plan.md

**Previous feature (implementada, pendiente de validación manual)**: Contenido enriquecido (formato, imágenes pegadas y adjuntos) en comentarios y descripción de Ticket/Tarea
**Spec**: specs/017-contenido-enriquecido-ticket/spec.md
**Plan**: specs/017-contenido-enriquecido-ticket/plan.md

**Previous feature (completada)**: Corregir el Cliente de un Usuario/cliente y desambiguar Proyectos homónimos
**Spec**: specs/016-corregir-cliente-encargado/spec.md
**Plan**: specs/016-corregir-cliente-encargado/plan.md

**Previous feature (completada)**: Encargado (Usuario/cliente) en múltiples Proyectos
**Spec**: specs/015-encargado-multiples-proyectos/spec.md
**Plan**: specs/015-encargado-multiples-proyectos/plan.md

**Previous feature (completada)**: SLAs por Proyecto y Prioridad (Fase 4 SDD V3)
**Spec**: specs/014-sla-tickets-tareas/spec.md
**Plan**: specs/014-sla-tickets-tareas/plan.md

**Previous feature (completada)**: Manejo Global de Errores y Notificaciones (API a Frontend)
**Spec**: specs/013-manejo-errores-notificaciones/spec.md
**Plan**: specs/013-manejo-errores-notificaciones/plan.md

**Previous feature (completada)**: Skills Requeridas en el Ticket
**Spec**: specs/011-ticket-skills-requeridas/spec.md
**Plan**: specs/011-ticket-skills-requeridas/plan.md

**Previous feature (completada)**: Cronómetro Manual de Tiempo en el Ticket (provisional)
**Spec**: specs/012-cronometro-manual-ticket/spec.md
**Plan**: specs/012-cronometro-manual-ticket/plan.md

**Previous feature (completada)**: Usuario/cliente por Proyecto, Asignación de Personal y Estructura de Skills
**Spec**: specs/010-proyecto-personal-skills/spec.md
**Plan**: specs/010-proyecto-personal-skills/plan.md

**Previous feature (completada, validada end-to-end contra Docker real)**: Listas de Tareas, Subtareas, ciclo de vida unificado y fix de Registro de tiempo
**Spec**: specs/009-tareas-listas-subtareas/spec.md
**Plan**: specs/009-tareas-listas-subtareas/plan.md

**Previous feature (completada)**: Fase 3 — Manejo de Tareas
**Spec**: specs/008-fase3-tareas/spec.md
**Plan**: specs/008-fase3-tareas/plan.md

**Previous feature (completada, validada end-to-end contra Docker real)**: Selección manual del Encargado solicitante en el Ticket
**Spec**: specs/007-ticket-encargado-cliente/spec.md
**Plan**: specs/007-ticket-encargado-cliente/plan.md

**Previous feature (implementada, pendiente de validación manual)**: Refactorización visual y de navegación del detalle del Ticket (flujo tipo Teamwork)
**Spec**: specs/006-ticket-detalle-tiempo-ui/spec.md
**Plan**: specs/006-ticket-detalle-tiempo-ui/plan.md

**Previous feature (completada)**: Registro de tiempo en el detalle del ticket, rol Encargado y navegación
**Spec**: specs/005-ticket-tiempo-encargado-nav/spec.md
**Plan**: specs/005-ticket-tiempo-encargado-nav/plan.md

**Previous feature (completada)**: Fase 2 — Registro diario de tiempos por recurso
**Spec**: specs/004-fase2-registro-tiempos/spec.md
**Plan**: specs/004-fase2-registro-tiempos/plan.md
**Data model**: specs/004-fase2-registro-tiempos/data-model.md

**Previous feature (completada)**: Fase 1 — Tickets
**Spec**: specs/002-fase1-tickets/spec.md
**Plan**: specs/002-fase1-tickets/plan.md
**Data model**: specs/002-fase1-tickets/data-model.md

**Sub-feature (completada)**: Reseteo de contraseñas y credenciales semilla
**Spec**: specs/003-reseteo-contrasenas/spec.md
**Plan**: specs/003-reseteo-contrasenas/plan.md

**Previous feature (completada)**: Fase 0 — Maestros
**Spec anterior**: specs/001-fase0-maestros/spec.md
**Data model maestros**: specs/001-fase0-maestros/data-model.md
**MER actual**: docs/MER.md
<!-- SPECKIT END -->
